#!/usr/bin/env spang2
# @title Count protein resources
# @endpoint agrold
# @option -vac --time

SELECT (COUNT(?s) AS ?count) ?graph ?class
WHERE {
  ?s a agrold:Protein .
  GRAPH ?graph {
    ?s a ?class .
  }
}
ORDER BY ?class DESC(?count)
